/* AG WORLD — live profile footer. Reads real user/profile/leaderboard data from API. */
(()=>{
const USER_ID='user-nico-van-rooyen';
const API=(window.AG_WORLD_CONFIG&&window.AG_WORLD_CONFIG.API_BASE_URL)||'https://ag-world-api.onrender.com';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const xpPct=(xp,next)=>next?Math.max(0,Math.min(100,(Number(xp)/Number(next))*100)):0;
async function load(){
 const [p,l]=await Promise.all([fetch(`${API}/api/users/${USER_ID}/profile`,{cache:'no-store'}),fetch(`${API}/api/users/${USER_ID}/leaderboard`,{cache:'no-store'})]);
 if(!p.ok||!l.ok)throw new Error('profile data unavailable');
 return {p:await p.json(),l:await l.json()};
}
function render({p,l}){const f=document.querySelector('.bottom');if(!f)return;f.classList.add('ag-profile-footer');const earned=p.achievements||[];const rewards=p.rewards||[];const next=rewards.find(r=>r.status==='locked');const pct=xpPct(p.xp,p.xpToNextLevel);const leaders=(l.leaders||[]).slice(0,5);f.innerHTML=`<section class="pf-section live-progress"><div class="pf-title">YOUR PROGRESS</div><div class="ag-progress-row"><div class="ag-level-shield"><b>${esc(p.level)}</b><span>LEVEL</span></div><div class="ag-progress-main"><strong>${Number(p.xp).toLocaleString()} / ${Number(p.xpToNextLevel).toLocaleString()} XP</strong><div class="ag-progress-bar"><i style="width:${pct}%"></i></div><div class="ag-progress-foot"><span>Level ${esc(p.level)}</span><span>${Math.round(pct)}%</span></div><div class="ag-progress-note">${next?`Next reward: ${esc(next.rewardName)}`:'All current rewards unlocked'}</div></div></div></section><section class="pf-section live-badges"><div class="pf-title">BADGES EARNED</div><div class="ag-badges">${earned.slice(0,5).map((a,i)=>`<div class="ag-badge-item"><div class="ag-badge ${i%3===1?'gold':i%3===2?'purple':''}">★</div><span>${esc(a.achievementName)}</span></div>`).join('')||'<div class="ag-progress-note">No achievements recorded yet</div>'}</div></section><section class="pf-section live-leaderboard"><div class="pf-title">${esc(l.territory||p.territory)} LEADERBOARD</div><div class="ag-leader-list">${leaders.map((r,i)=>`<div class="ag-leader-row ${r.id===p.id?'current':''}"><span class="ag-rank">${i+1}</span><span>${esc(r.name)}</span><span class="ag-leader-xp">${Number(r.xp).toLocaleString()} XP</span></div>`).join('')}</div><div class="ag-leader-link">Regional position: ${esc(p.regionalPosition??'—')}</div></section><section class="pf-section live-reward"><div class="pf-title">NEXT LEVEL REWARD</div><div class="ag-reward-drone"><div class="ag-rotor a"></div><div class="ag-rotor b"></div><div class="ag-drone-arm left"></div><div class="ag-drone-arm right"></div><div class="ag-drone-body"></div></div><div class="ag-reward-title">${next?esc(next.rewardName):'No pending reward'}</div><div class="ag-reward-sub">${next?.requiredLevel?`Unlocks at Level ${esc(next.requiredLevel)}`:'All current rewards unlocked'}</div><div class="ag-reward-bar"><i style="width:${pct}%"></i></div><div class="ag-reward-xp">${next?.requiredXp?`${Math.max(0,Number(next.requiredXp)-Number(p.xp)).toLocaleString()} XP to go`:'Complete'}</div></section>`;}
async function init(){try{render(await load());}catch(e){console.warn('AG World live footer:',e);const f=document.querySelector('.bottom');if(f)f.innerHTML='<section class="pf-section"><div class="pf-title">PROFILE DATA</div><div class="ag-progress-note">Live profile data is temporarily unavailable.</div></section>';}}
window.agWorldRefreshProfileFooter=init;document.addEventListener('DOMContentLoaded',init);setTimeout(init,1200);
})();
